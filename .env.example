NODE_ENV=development
PORT=3000

DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=u169101909_phamgia
DB_USER=phamgia_user
DB_PASSWORD=YOUR_DATABASE_PASSWORD

JWT_SECRET=phamgia_jwt_secret_change_this_2026

# Bảng đăng nhập (mặc định ưu tiên nguoi_dung)
USER_TABLE=nguoi_dung

# Google Drive OAuth (Cài đặt → Kết nối Google Drive)
APP_URL=http://localhost:3000
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
# Mặc định /api/google-drive/callback; nếu Google Console chỉ có /api/google/callback thì đổi:
# GOOGLE_REDIRECT_PATH=/api/google/callback
